type TodoItem {
    id: Int!
    text: String!
    completed: Boolean!
}

type TasksUpdate {
    tasks: [TodoItem!]!
}

enum Foo{
    BAR,
    BAZ
}

type Query {
    tasks: [TodoItem!]!
}

type Subscription {
    tasksUpdates: [TodoItem!]!
}