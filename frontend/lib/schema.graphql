type TodoItem {
    id: Int!
    text: String!
    completed: Boolean!
}

type TasksUpdate {
    type: String!
    task: TodoItem!
}

enum Foo{
    BAR,
    BAZ
}

type Query {
    tasks: [TodoItem!]!
}

type Subscription {
    tasksUpdates: TasksUpdate!
}